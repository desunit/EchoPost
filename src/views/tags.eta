<% layout("layout") %>

<h1>Tags</h1>
<p class="muted"><%= it.tags.length %> tags</p>
<ul class="tag-list">
  <% it.tags.forEach(function(t) { %>
    <li><a href="/tag/<%= t.slug %>"><%= t.name %></a> <span class="muted">(<%= t.post_count %>)</span></li>
  <% }) %>
</ul>
